<%= form_for(affiliation_type) do |f| %>
  <% if affiliation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(affiliation_type.errors.count, "error") %> prohibited this affiliation_type from being saved:</h2>

      <ul>
      <% affiliation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
