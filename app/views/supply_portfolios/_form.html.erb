<%= form_for(supply_portfolio) do |f| %>
  <% if supply_portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supply_portfolio.errors.count, "error") %> prohibited this supply_portfolio from being saved:</h2>

      <ul>
      <% supply_portfolio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_code %>
    <%= f.text_field :company_code %>
  </div>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
